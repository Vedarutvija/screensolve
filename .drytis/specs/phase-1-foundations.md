# Phase 1 — Foundations
## Goal
FastAPI + MySQL (SQLAlchemy), captures table, /health, env config, Caddy reverse proxy.
## Acceptance Criteria
- [ ] App loads in browser; GET /health returns ok JSON
- [ ] captures table exists in MySQL
## Edge cases
DB failure → clear log error.
